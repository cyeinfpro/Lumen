# Local Implementation Checklist (2026-09-05)

Base: `3278e6da2f5c9c31e0342a8e9fa58eb7c4d6c026`. Local repair only: no staging, commit, version bump, dependency mutation, publication, production access or provider calls. Original audit inputs are unchanged. Independent correctness/UI review and the targeted R10 recheck are complete. Parent accepted this local code candidate with the functional coverage and validation limits explicitly recorded below; this does not certify every proposed optimization or production scenario.

## Integration Sources

All eight lanes were integrated from `/private/tmp/lumen-0905-0542-{requests,server,canvas,interaction,theme,agent-ui,media,pages}` after checking base HEAD, empty staging, exact handoff file lists, binary tracked diffs and intended new files. Each tracked patch passed `git apply --check` before applying. No lane overlap or user-source conflict occurred. Bootstrap `.venv`/`node_modules` symlinks, ignored builds and scratch files were excluded.

Authoritative provenance is `/private/tmp/lumen-0905-0542/lane-manifests.json`, including every handoff path. Successful same-protocol canvas/agent-ui/media/pages recovery artifacts supersede prior aborted/timed-out runs; those are resolved tooling history, not remaining blockers. No alternate agents or execution protocols were used.

## Bugs

Paths below are relative to `apps/web/src` unless explicitly qualified. Implemented means live call sites are wired, not an unused reference component.

| ID | Disposition | Implementation / Evidence |
| --- | --- | --- |
| B01 | Implemented; review correction included | `features/agent/api/logicalAgentRequests.ts`, controller/submission helpers and existing Agent store retain payload-bound submission receipts in owner/session-scoped draft persistence. Unresolved retries retain their exact key after cross-tab shared-lease retirement. Matching real snapshots acknowledge only that session/key and clear unchanged submitted content, preserving subsequent edits. Node and mocked-browser cross-tab/reload/edited-draft tests plus PostgreSQL transaction tests pass. |
| B02 | Implemented | `apps/agent-runtime/src/server.ts` synchronously rechecks lifecycle after body/auth parsing, before capacity reservation/registration. Real HTTP barriers cover draining, readiness loss, destroyed/ended response, and accepted execution grace. |
| B03 | Implemented; browser render failure covered | `components/ErrorBoundary.tsx` and `LumenAppShell.tsx`: local retry, meaningful reset keys, feature-specific recovery and explicit refresh warning. `review-regressions.spec.ts` injects an actual optional preview render throw into served test chunks, recovers through the live boundary and verifies the composer draft remains. Failed dynamic import and live inpaint execution are not claimed. |
| B04 | Implemented; explicit generic type corrected | `lib/api/http.ts` dispatches HEAD through `queryClient.head`; return is `undefined`, no success-body parsing. Generic HEAD overload also returns `NoContent`; real `tsc` negative contracts reject JSON-body assignment/property access while preserving generic GET. Transport/auth/identity/request budgets remain covered. |
| B05 | Implemented | `apps/api/app/security.py`, `deps.py`, `config.py`: exact HMAC signature validation and ASCII Bot credentials; malformed headers reject through existing limiter and valid startup rules remain. ASGI/config tests pass. |
| B06 | Implemented | Existing `useModalLayer.ts` filters negative tabindex, disabled/fieldset, inert/hidden and invisible candidates. Browser Tab tests preserve one shared modal stack. |
| B07 | Implemented | `CommandPalette.tsx` rejects composition/repeat before Escape/global shortcut handling. Browser synthetic IME checks pass; real OS IME remains unverified. |
| B08 | Implemented | Palette reveals the actual active option inside its own scroll container after keyboard/filter/resize/responsive changes, without moving combobox focus. Browser geometry checks pass. |
| B09 | Implemented | `lib/canvas/documentMerge.ts` compares normalized projection versions without infinity subtraction. Missing/equal timestamps, sequence fallback, stale missing-item retention, reflexivity/antisymmetry/no-NaN tested. |
| B10 | Implemented; compatibility limit | Null-prototype canonical objects preserve own `__proto__` recursively. Ordinary persisted JSON fingerprints remain byte-compatible. Irreversibly colliding legacy own-`__proto__` payloads are intentionally not unsafely aliased. |
| B11 | Implemented | `app/globals.css`: Tailwind `source(none)` and stylesheet-relative `@source "../"`. Real production PostCSS tests cover source roots/exclusions/cwd equality. Formal Next builds pass from web cwd and root `--prefix`. No current cross-package class root exists. |
| B12 | Implemented at audited sites | Dedicated `--link-fg` used by Button/Markdown, readable palette grouping token; explicit/system light/dark tested. Link minimum contrast 5.05:1 on tested backgrounds. Other arbitrary legacy text/background combinations are not universally certified. |

## UI Recommendations

| ID | Disposition | Live Integration |
| --- | --- | --- |
| UI-01 | Implemented | Existing theme aliases, solid primary actions, opaque persistent chrome/composer, neutral non-shifting card hover. `apps/web/DESIGN.md` records adopted rules; no second palette or permanent v2 controller. |
| UI-02 | Implemented / existing protections retained | Existing Shell/navigation adapter, intrinsic narrow-desktop navigation, skip-main, responsive sidebars and one route scroll owner. Actual 768/820/1024/1440 navigation geometry passes. |
| UI-03 | Implemented | Agent summary opens one controlled parameter surface; mobile/desktop generation settings remain reachable; video prompt/action area is compact. Image high-frequency controls intentionally remain per current DESIGN contract. |
| UI-04 | Implemented | Agent tool calls are collapsible rows with ordered identities, persistent errors, sanitized details and available-artifact links. No fabricated execution records. |
| UI-05 | Implemented; pending reconcile corrected | Dedicated uncertain submission, accepted/active/stop-request/final states; later upload/file errors coexist. Reconciliation exposes its own pending flag and synchronous single-request fence without reusing initial message loading. Video uses truthful stages, persistent cancellation receipts and retry-epoch isolation. Costs remain estimates, not invented settlements. |
| UI-06 | Implemented | Agent/video use existing responsive overlays; parameter scope is next submission; unsupported model changes need confirmation. Controlled drafts/capability handlers are reused. |
| UI-07 | Implemented; actual owner fence verified | Canvas save/conflict status is persistent. Local-copy availability requires transaction ACK matching current edits AND a verified current-owner localStorage fence, not just a probe. Failed initial owner write remains unavailable after storage recovers; retry/export/adopt-remote/save-copy retain existing safety rules. |
| UI-08 | Implemented / existing primitives retained | EmptyState stays non-alert; ErrorState is alert with recovery. Asset/project/video/billing failures no longer masquerade as successful-empty results; cached assets survive refresh failure. |
| UI-09 | Implemented | Compact semantic project rows with real thumbnail/status/progress/update data and independent actions; billing audit and settlement tables. Existing UsersPanel table retained. |
| UI-10 | Implemented / existing performance retained | Asset preview, checkbox, retry and menu are sibling native controls; touch menu and original download/reference/copy/location/deletion remain reachable. Existing authenticated candidates, virtualization and bounded prewarm retained. |
| UI-11 | Implemented; prompt delete corrected | Async ConfirmDialog lock/pending/error with stable resource keys; project, prompt, API-key, device, account and billing recovery confirmations show target and impact. Prompt DELETE waits for confirmation and rejection keeps the target-specific dialog actionable. Project copy includes associated conversation/output cleanup and model-library preservation. |
| UI-12 | Implemented with explicit residual | One shared `primitives/UnsavedSettingsGuard.tsx` guards API-key, admin settings, Telegram and prompt drafts; mobile select and desktop admin navigation use the same callback. Prompt selection/new/close and app-link/unload departure are guarded. Prompt labels and field errors are connected; saved-credential probe errors are separate from replacement-field errors. Prompt route removes redundant outer border/padding/page-frame, preserving its actual editor, 44px controls, scrolling and safe area. Native same-document browser Back and unrelated programmatic navigation remain unguarded. |
| UI-13 | Implemented | Reduced-motion-aware JS scroll, one follow request per local message, prepend/history priority, content/viewport ResizeObserver and explicit target protection. Browser resize/long-output/disclosure tests pass. |

## Report Page And Architecture Coverage

| Report Section 5 Area | Disposition / Evidence |
| --- | --- |
| Global Shell | Implemented UI-02, B03/B06-B08; route visibility/authorization adapters unchanged. |
| Creation home | Existing immediately usable composer/real starters retained; UI-03/UI-06 adds reachable settings. Existing reference-upload browser matrix passes. No new landing page. |
| Agent | Implemented UI-03-06/UI-13; full existing Agent browser spec passes. |
| Image generation | Implemented reachable settings, next-submission scope and truthful estimates; existing submission/draft identity retained. |
| Video | Implemented truthful stages/cancellation and responsive controlled parameters; provider/OS execution is not mocked-browser evidence. |
| Canvas | Implemented durable save/conflict/recovery status; existing mutation/revision/atomic-group protocol retained. |
| Projects | Implemented compact rows and object-specific actions; unchanged route/feature protections. |
| Assets | Implemented independent interactions/thumbnail retry/error states; existing masonry and preview bounds retained. |
| Preview / inpaint | B03 local recovery plus shared focus fixes; existing lightbox/reference and model-library dialog browser regressions pass. No live inpaint generation claimed. |
| Settings | Implemented UI-12; native Back/programmatic-navigation gap is recorded above. Existing secret masking and query ownership retained. |
| Usage / billing | Implemented estimate/preauthorization/settlement distinction, canonical RMB formatting and error-aware real tables; no ledger semantics changed. |
| Admin | Existing permission enforcement and UsersPanel table retained; field accessibility, global scope, dirty guards, billing audit/actions implemented. Mocked provider discovery regression passes without a provider call. |
| Login / empty / error / offline | Existing authentication forms/routes and identity expiration recovery retained; shared and route error/empty work above is wired. Full live sign-in/provider lifecycle is outside this local mock run. |

Report sections 7-9: existing feature/API/model/controller boundaries, server projections, editor stores and local UI ownership are preserved; architecture and complexity gates have zero baseline debt and were not relaxed. Domain error projection uses exact codes/statuses, not guessed error-message substrings for execution uncertainty. No replacement state/router/UI library, speculative universal store, duplicate workbench controller, new rollout flag or benchmark claim was added. Route rollout/release staging is not applicable to this explicitly local repair. Performance trace baselines, 200% zoom/forced-colors and a physical-device keyboard/browser-toolbar matrix remain validation gaps, not claimed optimizations.

## Independent Review Dispositions

The two read-only reviews under `dfb71419-6c6d-4156-863a-5cd6c59bccb2/reviews/` originally blocked acceptance. All nine findings were accepted and implemented by the preceding final-fixes writer; recovery inspected those implementations without redoing the lanes. The subsequent independent correctness review cleared R01-R03 and found no additional source-proven P1/P2. The subsequent UI review accepted the six prior UI corrections and keyboard-admin/B03 evidence, but identified one remaining P2 in R09's success path, recorded as R10 below. Fresh reviewer `795e9b63-5b8d-4eb5-9b11-f604b27a583d` verified R10 against current source, the red/green browser regression and final evidence, returning `clear` with no findings. Parent verified the reviewed file hashes, accepted R01-R10 as resolved, and retained the documented functional and validation limits.

| Review | Disposition | Current Implementation And Regression Evidence |
| --- | --- | --- |
| R01 Correctness P1, cross-tab replay/draft acknowledgement | Corrected | `logicalAgentRequests.ts` replays the retained draft receipt key; `agentSubmission.ts`, `submissionReceipts.ts`, owner-scoped `draftPersistence.ts` and session-fenced store acknowledgement preserve the owner/session/payload relationship. `agent-ui-audit.spec.ts` covers two tabs, accepted/completed replay with delayed A snapshots, reload acknowledgement and subsequent edits; `submissionReceipts.test.ts` covers owner mismatch and real matching keys. |
| R02 Correctness P2, generic HEAD body contract | Corrected | Generic HEAD overload returns `NoContent`; `httpTypeContracts.test.ts` is checked by the repository `tsc` gate; negative contracts reject body promises/property access, preserving generic GET. Runtime transport regressions remain. |
| R03 Correctness P2, canvas owner fence | Corrected conservatively | `canvasEmergencyStorageAvailable` requires the actual stored owner to match the active private owner. It does not repair an uncertain fence outside the identity lifecycle. `canvas-durability.spec.ts` fails the initial owner write, restores storage, retries, and verifies no false availability claim before reload; tests explicitly do not claim the unavailable draft survives owner cleanup. |
| R04 UI P1, mobile admin select bypass | Corrected | Both navigation modes use `requestSettingsNavigation`; settings/Telegram mount the one shared guard. `review-regressions.spec.ts` verifies cancel retains edits and confirm unmounts them. Recovery repeats keyboard phone/touch phone/desktop three times each. |
| R05 UI P1, prompt/Telegram explicit-save drafts | Corrected | Prompt selection/new/close and app-route departure use `useUnsavedSettingsGuard`; Telegram registers the shared guard. Browser tests verify cancellation and confirmed departure. Existing stores/mutations retain draft ownership. |
| R06 UI P2, prompt labels/field errors | Corrected | `SystemPromptManagerPresentation.tsx` uses stable IDs, `htmlFor`, field-specific `aria-invalid` and `aria-describedby`. Browser invalid-save tests locate controls by their visible labels and assert associated errors. |
| R07 UI P2, prompt deletion | Corrected | Existing `ConfirmDialog`, keyed by prompt ID, names target and actual unlinking impact; DELETE only after confirm. Browser checks zero requests before confirm/cancel and a retained actionable dialog after rejection. |
| R08 UI P2, pending reconciliation | Corrected | Controller exposes `checkingSubmission`, guarded by a synchronous ref and non-cancelling refetch; UI disables/shows the pending action independently of initial loading. Held-GET browser case verifies one request, retained draft and recovery. |
| R09 UI P2, credential probe errors | Corrected; follow-up R10 included | Existing-credential probe errors stay with health/actions; replacement input uses its own validation/save error. Browser failure with untouched replacement verifies validity and that typing does not clear the probe failure. |
| R10 final UI P2, stale probe error after replacement | Corrected; independent recheck clear | `settings/api-key/page.tsx` clears the existing `probeError` alongside `probeMessage` only when credential replacement succeeds. The extended saved-credential browser test checks the PUT method/body, refreshed healthy replacement hint, empty valid replacement field and removal of the old rejection alert; typing alone still retains it. Before the one-line product fix the test failed exactly on the retained old alert (`remaining-repro.log`); afterward all seven viewport projects pass within the 70-case affected matrix. No server/draft/identity contract or duplicate state was introduced. |

Additional approved cleanup removes only the prompts route's redundant decorated wrapper and duplicate width/insets. Save is scrolled into view in real browser tests; full intersection, minimum 44px height and center hit-target checks run before screenshots. B03's supplementary actual render-failure test is also included.

## Checks And Evidence

All logs and browser artifacts are outside the repo in `/private/tmp/lumen-0905-0542`. `recovery-*` artifacts supersede earlier integration/fixes counts for the pre-R10 snapshot. Their detailed results below remain historical evidence, not a claim that the entire matrix/backend suite was rerun after R10. `remaining-*` records the exact final bounded R10 validation. Independent review and parent code acceptance are complete; the whole browser/backend suite was not rerun for the final one-line UI correction.

### Final Bounded R10 Validation

- Product delta is one `setProbeError(null)` call in successful replacement; only `settings/api-key/page.tsx`, `e2e/review-regressions.spec.ts` and this checklist differ from `remaining-before-integrated-files.json`. No previously completed lane was reopened.
- Red/green proof: `remaining-repro.log` has **1 expected failed desktop case** before the product fix, after successful PUT, empty field and visible refreshed credential; it fails because the old rejection alert remains. Trace/screenshot are retained under `remaining-repro/`. This is historical failure evidence, not a pass.
- Exact final affected mocked matrix: **70 passed / 0 skipped / 0 failed**, all `review-regressions.spec.ts` and `pages-audit.spec.ts` cases on seven projects (`remaining-playwright.log`, `remaining-playwright/`). Includes R04-R07/R09-R10, optional preview recovery and existing API-key save-failure/dirty-input coverage. No paid-provider suite runs.
- Exact final web unit suite: **855 passed / 0 failed / 0 skipped** (`remaining-web-test.log`). Full lint/architecture/layout/governance/complexity pass (`remaining-web-lint.log`). Production `npm --prefix apps/web run build` passes, followed serially by `npm --prefix apps/web run type-check` (`remaining-web-build.log`, `remaining-web-types-serial.log`). No generated tsconfig changes remain. API-key dark-utility scan has zero matches.
- **91 PNG attachments** from the current affected matrix are persisted in `remaining-screenshots/manifest.json`; decoded nonblank/pixel/hash checks are in `remaining-screenshot-pixels.json`. Replacement-state screenshots inspected on 320 light, 375 dark and desktop. Phone captures are scrolled viewports, not claims that the entire form fits on one screen.
- `remaining-integrity.json` checks the bounded three-file delta, current source hashes, unchanged original audit inputs and empty staging. `integrated.patch` and `integrated-files.json` are regenerated for the actual final candidate: **134 files / 98 tracked modifications / 36 intended new files**, with SHA256 and new-file contents, plus successful reverse-apply check. Prior snapshot is retained as `remaining-before-integrated.patch` and `remaining-before-integrated-files.json`.
- Runtime113, Python553, disposable PostgreSQL4 and full mocked **445 passed / 80 intentional skips** remain the preceding recovery evidence below; those sources are hash-identical after this UI-only fix. These suites were not rerun in this bounded turn. The affected 70 cases are not added to 445 as a new full-matrix count.
- Owned preview remains `http://127.0.0.1:3142`, listening PID88525; no service restart, user3000 access, PostgreSQL restart, staging, commit, version/dependency/publication or production access occurred. Existing documented navigation/storage/provider/OS/performance limits remain unchanged.

### Pre-R10 Recovery Validation

- Exact final web `npm test`: **855 passed**, zero failed/skipped (`recovery-web-test.log`), +52 over the 803-test baseline. Full `npm run lint` passes (`recovery-web-lint.log`), with all layout/UI-governance/architecture/complexity rules retained. Typecheck passes serially AFTER both builds (`recovery-web-types-serial.log`), avoiding generated Next type races.
- Final `npm run build` from `apps/web` and root `npm --prefix apps/web run build` both pass (`recovery-web-build.log`, `recovery-root-web-build.log`). Real CSS/source/contrast compiler tests remain in the full Node suite.
- Runtime rerun: **113 passed / 13 files**, plus type-check/lint/build passed (`recovery-runtime-{test,types,lint,build}.log`). No provider calls.
- Python rerun with explicit current-worktree `PYTHONPATH="$PWD/apps/api:$PWD/packages/core"`: **553 passed** across auth/config/CSRF/security/rate-limit/video serialization/media/Agent foundation/workflows (`recovery-python.log`). This includes the 52 Agent foundation tests; no extra count is added for earlier separate reruns.
- Exact final Agent transactions rerun: **4 PostgreSQL tests passed** (`recovery-postgres.log`) against only owned disposable `127.0.0.1:55442`, role `lumen_audit`. Board-authorized exact instance restarted after unused-port check and stopped afterward (`recovery-postgres-start.log`, `recovery-postgres-stop.log`). Existing port 5432 untouched; provider/Redis test doubles do not prove live billing.
- Affected final-source prompt/admin browser cases: **45 passed / 0 failed** (`recovery-affected.log`, three repeats on phone touch, phone keyboard and desktop). Prompt Save checks retain all dirty/confirm/error assertions and add full-viewport/minimum-hit-size/non-occlusion assertions.
- Full final mocked browser matrix: **445 passed / 80 intentional viewport-project skips / 0 failed**, **525 cases**, **18.3 minutes** (`recovery-playwright-final.log`, `recovery-playwright-final/`). All seven configured projects (320 light, 375 touch dark, 700x320 landscape dark, 375 keyboard dark, 768 light, 1024 dark, 1440 light), including all existing mocked specs and review regressions. `agent-live.spec.ts` intentionally excluded. Prior 361-pass integration and 444/80/1 fixes matrices are historical, not final proof. All test processes are terminal.
- **155 current-source PNG attachments** persisted in `recovery-screenshots/manifest.json`, all decoded and nonblank (`recovery-screenshot-pixels.json`, `recovery-evidence.log`). Actual prompt Save screenshots inspected on 320 light, 375 touch dark and 1440 desktop light, plus keyboard-phone retained admin draft. Save full intersection, 44px height and center hit-target checks pass in every project. Earlier `screenshots/` and `final-screenshots/` remain historical.
- `recovery-integrity.json` verifies all **133 non-checklist candidate files** still match their tested-source export, all original audit hashes, and empty staging. Final manifest includes **134 files: 98 tracked modifications and 36 intended new source/test/doc files**, with new-file contents and SHA256, excluding original audit inputs/generated state. Regenerated patch reverse-apply check passes.
- `git diff --check` passes; staged path list is empty. Source patch including intended new files: `integrated.patch`; exact file/hash/provenance and intended new-file CONTENT manifest: `integrated-files.json` (regenerated from the provider-503 checkpoint scope, not stale lane paths). Original untracked audit documents are excluded except this new checklist. All nine original input hashes pass (`recovery-original-audit.log`).
- Hard-coded dark utility scan: only three existing `text-white` variants in the intentional video media stage (`recovery-theme-dark-scan.log`); none in the prompt/guard/admin/credential surfaces changed by review fixes. No version/dependency/staging/publication changes or generated Next config churn.

### Integration Corrections And Failed-Run History

Earlier review-fix attempts remain recorded: `fixes-test-first.log` failed one new receipt test's alias import; `fixes-lint-first.log` rejected a component importing the app-local guard. The current test loader and single shared primitive resolve those failures without baseline relaxation; the app-local guard remains removed. `fixes-browser-first.log` used an incorrect model label; `fixes-browser-triage.log` retained mobile confirmation/preview failures, followed by four passes in `fixes-browser-triage2.log`. Current final gates, not those partial runs, determine acceptance.

Provider-503 recovery: child `105bbc6c-6b04-491a-af45-1591c20f9451` failed after partial fixes, with process terminal and no retained running tests. Parent captured `provider503-checkpoint.patch/json`; this is a SAME-NATIVE-PROTOCOL single-writer recovery, not an execution fallback or lane reintegration. Its last matrix was **444 passed / 80 intentional skips / 1 failed** (`fixes-playwright-final.log`, 525 total); it was never accepted as fully green.

The remaining dirty-admin keyboard failure reproduced **5/5** (`recovery-repro.log`, retained traces). Isolated native-select probes show non-mobile headless Chromium emits `keydown` but NO `input`/`change` for ArrowUp; the same key changes selection in mobile emulation. Type-ahead emits real keyboard-driven `input`/`change` in both (`recovery-native-select-modes.log`). The test now uses `t` to select Telegram on keyboard phone and retains ArrowUp on touch, with all existing dialog/cancel/draft/confirm assertions unchanged. No guard was weakened, event synthetically dispatched, timeout increased, or failure classified as a race. The corrected affected cases pass three repeats on each target mode.

The approved controller correction suppresses only duplicate feedback for the exact retained current-session uncertain optimistic attempt. It leaves unmarked session/continuation errors and later upload/file errors visible. Store tests and real 504/reload browser tests cover the guard. The existing geometry test now uses the stable composer test id instead of removed glass styling.

Actual browser discovery: mobile palette restored focus before the exiting BottomSheet released its trap/inert. Optional `onExitComplete` now runs after exiting-layer cleanup following the existing shared modal cleanup; palette waits a task, rejects stale/reopened/disconnected/inert targets and does not restore on route commands. Normal/reduced motion, Escape/close, quick reopen and route navigation pass.

Resolved test-fixture issues: Agent details exceeded the existing six-snippet DTO bound; equal user/assistant timestamps reversed fixture chronology; pages assertions matched Next's route announcer or intentionally inert background; canvas polled an emergency copy already retired after a successful IDB ACK; legacy continuation key prefixes no longer matched durable keys; fixture auth/capability statuses differed from real backend 412; desktop native select consumed the first Escape, so the parameter test now Tabs out before testing popup Escape. No validator, safety assertion or governance baseline was weakened. Initial/retriage logs are preserved as `playwright-new.log`, `playwright-triage.log`, `playwright-integrated.log`, and `playwright-agent-triage.log`.

An initial new controller branch exceeded the unchanged complexity budget and was extracted into the existing submission helper. An initial nonstandard dev build directory polluted ESLint discovery; only owned generated output was moved outside the repo and standard ignored `.next-e2e` used. A typecheck/build concurrency run saw transient missing generated Next types (`web-final-types.log`); serial rerun passed. Next-generated tsconfig changes were restored without altering original config.

Representative current screenshots (all under `/private/tmp/lumen-0905-0542/recovery-screenshots/`):

- [320 light prompt Save](/private/tmp/lumen-0905-0542/recovery-screenshots/phone-320-light-review-regressions-spec-ts-review--dirty-prompt-selection--creation-and-route-departure-preserve-drafts-until-confirmed-prompt-retained-draft-a28f032e9844.png)
- [375 dark prompt Save](/private/tmp/lumen-0905-0542/recovery-screenshots/phone-375-dark-review-regressions-spec-ts-review--dirty-prompt-selection--creation-and-route-departure-preserve-drafts-until-confirmed-prompt-retained-draft-49341918d4cf.png)
- [Desktop prompt Save](/private/tmp/lumen-0905-0542/recovery-screenshots/desktop-light-review-regressions-spec-ts-review--dirty-prompt-selection--creation-and-route-departure-preserve-drafts-until-confirmed-prompt-retained-draft-1f89e426cc97.png)
- [Keyboard-phone admin retained draft](/private/tmp/lumen-0905-0542/recovery-screenshots/phone-375-keyboard-dark-review-regressions-spec-ts-review--dirty-admin-settings-guards-mobile-select-and-desktop-navigation-admin-settings-dirty-6ce5e2966229.png)

## Final Parent Acceptance

Final workflow `dbff23fa-ed38-4085-850c-f4e4e720b13c` is complete; every child is terminal. Correctness reviewer `57c42ad0-7b6a-4f5c-8a26-e4de8b9d24e1` returned `clear` for R01-R03 and the reviewed server/runtime scope. UI reviewer `6ec83bee-64e9-4b8f-9e45-a19d674a551b` accepted R04-R09 and found R10; final recheck `795e9b63-5b8d-4eb5-9b11-f604b27a583d` returned `clear` after the bounded R10 fix. No source-proven P1/P2 from these reviews remains unresolved.

Parent independently checked all 134 candidate hashes, the patch hash and reverse-apply check, unchanged original nine audit inputs, empty staging, `git diff --check`, `python3 scripts/version.py check` (1.2.157), stopped owned PostgreSQL and live preview health. Representative mobile Agent/canvas/prompt and desktop prompt screenshots were inspected. Product source is unchanged after the final reviewer; this section only records acceptance. Structured reviewer decisions and parent verification are saved in `/private/tmp/lumen-0905-0542/final-acceptance.json`; refreshed patch and hash/content manifest remain `integrated.patch` and `integrated-files.json` there.

Acceptance is for the documented local repair candidate, not a claim that all UI optimization coverage is universal: native Back/unrelated programmatic dirty navigation remains a known functional gap. The remaining storage, compatibility, physical-device, provider and measurement limits below are also retained. No commit, version bump, push, tag, deployment or release was performed.

## Running Preview And Remaining Limits

Final preview: **http://127.0.0.1:3142**, launcher PID **88524**, listening Next PID **88525**, log `/private/tmp/lumen-0905-0542/preview.log`, metadata `preview.json`. Port was checked unused before each owned launch. Existing port 3000 was not touched. Backend is deliberately `http://127.0.0.1:9`; application data is available through mocked Playwright fixtures, not a connected production/provider environment. Original ignored `.next-e2e` output is preserved at `original-next-e2e/` outside the repo.

True remaining limits: native Back/programmatic dirty-navigation gap; legacy own-`__proto__` fingerprint compatibility; no real OS IME, forced-colors/200% zoom/physical-device keyboard testing; no provider billing/cancellation or production integration certification; no performance benchmark; no guarantee against OS termination/storage eviction. Failed initial canvas owner writes deliberately keep recovery unavailable: export is the recovery path, and reload may discard an unfenced draft. JSON canvas export is a recovery artifact, not a new automatic importer. Explicit refresh may discard unsaved in-memory data and is labeled accordingly. B03 browser evidence covers an actual optional render throw, not a failed dynamic import. Owned PostgreSQL is stopped; preview is unchanged and mock-only.
